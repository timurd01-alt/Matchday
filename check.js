
const $=s=>document.querySelector(s);let DATA={matches:[],news:[],standings:[]},BYID={},VIEW='matches',LAST_OK=false,LAST_ERROR='',LOAD_TIMER=null,NEWS_FILTER='all';
const DEFAULT_SETTINGS={accent:'orange',density:'normal',panel:'glass',defaultView:'matches',refresh:60,showInsight:true,showFinished:false,showDetails:true};
let SETTINGS={...DEFAULT_SETTINGS};try{SETTINGS={...DEFAULT_SETTINGS,...JSON.parse(localStorage.getItem('matchday.settings')||'{}')}}catch(e){}
const COLORS={orange:'#ffb02e',blue:'#4cc2ff',green:'#3ad17a',red:'#ff4d5e',purple:'#b16cff'};
function saveSettings(){localStorage.setItem('matchday.settings',JSON.stringify(SETTINGS))}
function applySettings(){document.documentElement.style.setProperty('--signal',COLORS[SETTINGS.accent]||COLORS.orange);document.body.classList.toggle('compact',SETTINGS.density==='compact');document.body.classList.toggle('spacious',SETTINGS.density==='spacious');$('#app').classList.toggle('flat',SETTINGS.panel==='flat');$('#app').classList.toggle('noinsight',!SETTINGS.showInsight);document.body.classList.toggle('hideStats',!SETTINGS.showDetails)}
function updateSetting(k,v){if(k==='showInsight'||k==='showDetails'||k==='showFinished')v=!!v;if(k==='refresh')v=Number(v);SETTINGS[k]=v;saveSettings();applySettings();renderCurrent();scheduleNextLoad()}
function resetSettings(){SETTINGS={...DEFAULT_SETTINGS};saveSettings();applySettings();setView(SETTINGS.defaultView);scheduleNextLoad()}
function esc(s){return String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function dt(iso){try{return new Date(iso).toLocaleString([],{weekday:'short',hour:'numeric',minute:'2-digit',month:'short',day:'numeric'})}catch(e){return''}}
function ago(iso){try{const s=(Date.now()-new Date(iso).getTime())/1000;if(!isFinite(s))return'';if(s<70)return'just now';if(s<3600)return`${Math.round(s/60)}m ago`;if(s<86400)return`${Math.round(s/3600)}h ago`;return`${Math.round(s/86400)}d ago`}catch(e){return''}}
function kickIn(iso){try{const m=Math.round((new Date(iso)-Date.now())/60000);return m<=0?'now':m<60?`in ${m}m`:m<1440?`in ${Math.round(m/60)}h`:`in ${Math.round(m/1440)}d`}catch(e){return''}}
const STALE_MATCH_MINUTES=150;
function kickMs(m){const t=Date.parse(m?.kickoff||'');return Number.isFinite(t)?t:0}
function isStaleUpcoming(m){const t=kickMs(m);return m?.status==='UPCOMING'&&t>0&&(Date.now()-t)>STALE_MATCH_MINUTES*60000}
function isCompleteOrPast(m){return m?.status==='FINISHED'||isStaleUpcoming(m)}
function isVisibleUpcoming(m){return m?.status==='UPCOMING'&&!isStaleUpcoming(m)}
function fixtureSort(a,b){const o={LIVE:0,UPCOMING:1,FINISHED:2};return (o[a.status]??9)-(o[b.status]??9)||(a.kickoff||'').localeCompare(b.kickoff||'')}
function scoreText(m){const done=m.status==='FINISHED'||m.status==='LIVE';if(isStaleUpcoming(m))return'<span class="kick">Past kickoff</span>';return done?`${m.score?.home??'-'}<span class="sep">–</span>${m.score?.away??'-'}`:`<span class="kick">${dt(m.kickoff).split(', ').pop()||'TBD'}</span>`}
function statNum(v){const m=String(v??'').match(/-?\d+(\.\d+)?/);return m?Number(m[0]):0}
function pressure(stats,side){if(!stats)return 0;const s=stats[side]||{};return statNum(s.shots_on_target)*4+statNum(s.shots)*1.2+statNum(s.corners)*1.4+statNum(String(s.possession).replace('%',''))*.08-statNum(s.red_cards)*4}
function statRow(label,h,a){const hn=statNum(h),an=statNum(a),tot=Math.max(1,hn+an);return `<div class="home">${esc(h||'-')}</div><div class="lab">${label}</div><div class="away">${esc(a||'-')}</div><div class="statbar"><i style="width:${hn/tot*100}%"></i><i style="width:${an/tot*100}%"></i></div>`}
function pct(v){v=Number(v);return Number.isFinite(v)?Math.max(0,Math.min(100,Math.round(v))):0}
function bar1x2(h,d,a){h=pct(h);d=pct(d);a=pct(a);return `<div class="bar"><div class="seg h" style="flex-basis:${h}%"><span>${h}%</span></div><div class="seg d" style="flex-basis:${d}%"><span>${d}%</span></div><div class="seg a" style="flex-basis:${a}%"><span>${a}%</span></div></div>`}
function duo(xl,xv,yl,yv){xv=pct(xv);yv=pct(yv);return `<div class="mkt"><div class="lbls"><span>${esc(xl)} <b>${xv}%</b></span><span><b>${yv}%</b> ${esc(yl)}</span></div><div class="duo"><i class="x" style="flex-basis:${xv}%">${xv}%</i><i class="y" style="flex-basis:${yv}%">${yv}%</i></div></div>`}
function marketPanel(m){const mk=m.markets||{},x=mk['1x2']||{};let h='<div class="seclbl">Odds tracker</div>';if(x.home_pct!=null){h+=`<div class="problbl"><span>${esc(m.home.code||m.home.name)} win</span><span>draw</span><span>${esc(m.away.code||m.away.name)} win</span></div>${bar1x2(x.home_pct,x.draw_pct,x.away_pct)}<div class="faintline" style="margin-top:6px">1X2 market · ${x.books||'?'} books</div>`}else h+='<div class="nomk">No 1X2 market odds yet.</div>';if(mk.totals)h+=`<div class="seclbl">Goals — over/under ${esc(mk.totals.line)}</div>`+duo(`Over ${mk.totals.line}`,mk.totals.over_pct,`Under ${mk.totals.line}`,mk.totals.under_pct);return h}
function modelBlock(m){const pr=m.prediction;if(!pr)return '<div class="seclbl">Model pick</div><div class="nomk">No model pick yet.</div>';const cls=pr.edge!=null&&Math.abs(pr.edge)>=6?'edge':'';return `<div class="seclbl">Model pick</div><div class="pick ${cls}"><span class="pl">Pick</span><span class="pn">${esc(pr.pick_name)}</span><span class="pc">${pr.confidence}%</span><span class="pnote">${esc(pr.note||'')}</span></div>`}
function edgeBreakdown(m){const pr=m.prediction,x=(m.markets||{})['1x2']||{};if(!pr)return '';const mkmap={h:x.home_pct,d:x.draw_pct,a:x.away_pct};const side=pr.pick,modelP=pr.model?pr.model[side]:null,mktP=mkmap[side],edge=pr.edge;const t=(side==='h')?m.home:(side==='a')?m.away:null;let bits=[];if(edge!=null&&mktP!=null&&modelP!=null){if(edge>=6)bits.push(`The model rates ${pr.pick_name} higher than the market (${modelP}% vs ${mktP}%).`);else if(edge<=-6)bits.push(`The model is cooler on ${pr.pick_name} than the market (${modelP}% vs ${mktP}%).`);else bits.push(`Model and market broadly agree (${modelP}% vs ${mktP}%).`)}else bits.push(`Model pick is ${pr.pick_name} at ${pr.confidence}%.`);if(t){const f=(t.form||'').split(' ').filter(Boolean);if(f.length)bits.push(`${t.name} form: ${f.join(' ')} · GD ${t.gd>0?'+':''}${t.gd}.`)}const tot=(m.markets||{}).totals;if(tot)bits.push(`Goals market: over ${tot.line} ${tot.over_pct}%, under ${tot.line} ${tot.under_pct}%.`);return bits.join(' ')}
function fmtStat(v){return (v===undefined||v===null||v==='')?'-':String(v)}
function statMetric(label,h,a){const hn=statNum(h),an=statNum(a),tot=Math.max(1,hn+an),hw=hn||an?Math.max(4,hn/tot*100):50,aw=hn||an?Math.max(4,an/tot*100):50;return `<div class="statMetric"><div class="val home">${esc(fmtStat(h))}</div><div class="mid"><div class="lab"><span>${esc(label)}</span><span>${hn>an?esc(DATA_HNAME||'Home'):an>hn?esc(DATA_ANAME||'Away'):'even'}</span></div><div class="metricBar"><i style="width:${hw}%"></i><i style="width:${aw}%"></i></div></div><div class="val away">${esc(fmtStat(a))}</div></div>`}
let DATA_HNAME='',DATA_ANAME='';
function teamSnap(team,side){const gd=Number(team?.gd||0),form=String(team?.form||'').trim();return `<div class="teamSnap ${side==='away'?'away':''}"><div class="snapCode">${esc(team?.code||side)}</div><div class="snapName">${esc(team?.name||'TBD')}</div><div class="snapMeta">${team?.pos?`#${team.pos} · `:''}${team?.pts??0} pts · GD ${gd>0?'+':''}${gd}${form?` · ${esc(form)}`:''}</div></div>`}
function statsPanel(m){DATA_HNAME=m.home?.code||'Home';DATA_ANAME=m.away?.code||'Away';const sx=m.stats_extra,hs=sx?.home||{},as=sx?.away||{};const ph=sx?pressure(sx,'home'):0,pa=sx?pressure(sx,'away'):0;const leader=!sx?'Waiting':ph===pa?'Balanced':ph>pa?(m.home?.code||m.home?.name):(m.away?.code||m.away?.name);let html=`<div class="statsBoard"><div class="seclbl">Match read</div><div class="matchRead">${teamSnap(m.home,'home')}<div class="snapMid"><span>${esc(m.status||'')}</span><b>${scoreText(m).replace(/<[^>]+>/g,'–')}</b><span>${esc(m.stage||'')}</span></div>${teamSnap(m.away,'away')}</div>`;if(sx){html+=`<div class="statHero"><div class="pressureChip"><div class="label">Pressure index</div><div class="value">${Math.round(ph)}–${Math.round(pa)}</div><div class="sub">${esc(leader)} ${leader==='Balanced'?'match':'lean'} · not xG</div></div><div class="pressureChip"><div class="label">Best public signal</div><div class="value">${esc(leader)}</div><div class="sub">based on shots, SOT, possession, corners and cards</div></div></div><div class="statMetrics">${statMetric('Shots',hs.shots,as.shots)}${statMetric('Shots on target',hs.shots_on_target,as.shots_on_target)}${statMetric('Possession',hs.possession,as.possession)}${statMetric('Corners',hs.corners,as.corners)}${statMetric('Fouls',hs.fouls,as.fouls)}${statMetric('Offsides',hs.offsides,as.offsides)}${statMetric('Saves',hs.saves,as.saves)}${statMetric('Cards',`${hs.yellow_cards||0}Y ${hs.red_cards||0}R`,`${as.yellow_cards||0}Y ${as.red_cards||0}R`)}</div>`}else{html+=`<div class="emptyStats">Box-score stats — yet.</div>`}return html+`</div>`}
function lineupsPanel(m){if(m.lineups)return `<div class="lineupBoard"><div class="seclbl">Lineups</div><div class="xi"><div><h4>${esc(m.home.code||m.home.name)} ${esc(m.lineups.home.formation||'')}</h4>${(m.lineups.home.xi||[]).map(p=>`<div class="player ${p.out?'out':''}">${esc(p.n)} ${esc(p.name)}</div>`).join('')}</div><div><h4>${esc(m.away.code||m.away.name)} ${esc(m.lineups.away.formation||'')}</h4>${(m.lineups.away.xi||[]).map(p=>`<div class="player ${p.out?'out':''}">${esc(p.n)} ${esc(p.name)}</div>`).join('')}</div></div></div>`;return `<div class="lineupBoard"><div class="seclbl">Lineups</div><div class="emptyStats">Lineups — yet.</div></div>`}
function details(m){return `<div class="detailGrid"><div class="detailTop"><div class="readCard">${modelBlock(m)}</div><div class="readCard">${marketPanel(m)}</div></div>${statsPanel(m)}${lineupsPanel(m)}</div>`}
function cardHTML(m){const live=m.status==='LIVE',stale=isStaleUpcoming(m),displayStatus=stale?'PAST / REFRESH':m.status;const x=(m.markets&&m.markets['1x2'])||{};const probTop=x.home_pct!=null?`<div class="prob"><div class="problbl"><span>${esc(m.home.code||m.home.name)}</span><span>Market read</span><span>${esc(m.away.code||m.away.name)}</span></div>${bar1x2(x.home_pct,x.draw_pct,x.away_pct)}</div>`:`<div class="prob"><div class="nomk">No market odds yet</div></div>`;const pr=m.prediction;const pick=pr?`<div class="pick ${pr.edge!=null&&Math.abs(pr.edge)>=6?'edge':''}"><span class="pl">Pick</span><span class="pn">${esc(pr.pick_name)}</span><span class="pc">${pr.confidence}%</span><span class="pnote">${esc(pr.note||'')}</span></div>`:'';return `<article class="card" data-id="${esc(m.id)}"><div class="head" onclick="this.parentElement.classList.toggle('open')"><div class="metarow"><span class="stage">${esc(m.stage||'Fixture')}</span><span class="spacer"></span><span class="pill ${esc(displayStatus)}">${live?'<span class="blink"></span>':''}${esc(displayStatus)}</span></div><div class="fixture"><div class="side"><div class="tname">${esc(m.home.name)}</div><div class="tsub"><span>${esc(m.home.code)}</span><span>${m.home.pos?`#${m.home.pos}`:''}</span><span>${m.home.pts??0} pts</span></div></div><div class="center"><div class="score">${scoreText(m)}</div>${live?`<div class="minute">${m.minute||''}'</div>`:`<div class="kick">${stale?'past kickoff':kickIn(m.kickoff)}</div>`}</div><div class="side away"><div class="tname">${esc(m.away.name)}</div><div class="tsub"><span>${esc(m.away.code)}</span><span>${m.away.pos?`#${m.away.pos}`:''}</span><span>${m.away.pts??0} pts</span></div></div></div>${probTop}${pick}<div class="expander">model · odds · stats · lineups</div></div><div class="detail">${details(m)}</div></article>`}
function renderMatches(){const M=DATA.matches||[];const hiddenPast=M.filter(isCompleteOrPast).length;let list=M.filter(m=>!isCompleteOrPast(m)||SETTINGS.showFinished);list.sort(fixtureSort);const tg=`<label class="toggle"><input type="checkbox" ${SETTINGS.showFinished?'checked':''} onchange="updateSetting('showFinished',this.checked)"> show completed / past matches</label>`;const note='';$('#view-matches').innerHTML=`<div class="vhead">Fixtures</div>`+(list.length?list.map(cardHTML).join(''):`<div class="empty" style="grid-column:1/-1">No live or upcoming matches to show.</div>`)+tg}
function groupLetter(g){return String(g||'').replace(/^Group\s*/i,'').replace(/^GROUP_/i,'').trim()}
function cleanGroup(g){g=String(g||'').trim();if(!g)return'';if(/^GROUP_/i.test(g))return g.replace('GROUP_','Group ').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());if(/^Group\s+/i.test(g))return 'Group '+groupLetter(g).toUpperCase();return g}
function rowKey(n){return String(n||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim()}
function ensureRow(map,team,group){const key=rowKey(team?.name);if(!key)return null;if(!map[key])map[key]={name:team?.name||'',code:team?.code||'',group:cleanGroup(group||team?.group),pld:0,w:0,d:0,l:0,gf:0,ga:0,gd:0,pts:0,form:'',live:false,results:[]};else{map[key].code=map[key].code||team?.code||'';map[key].group=map[key].group||cleanGroup(group||team?.group)}return map[key]}
function addResult(row,gf,ga,live,kick){row.pld++;row.gf+=gf;row.ga+=ga;row.gd=row.gf-row.ga;if(gf>ga){row.w++;row.pts+=3;row.results.push([kick,'W'+(live?'*':'')])}else if(gf<ga){row.l++;row.results.push([kick,'L'+(live?'*':'')])}else{row.d++;row.pts+=1;row.results.push([kick,'D'+(live?'*':'')])}row.live=row.live||live}
function deriveStandings(){if(Array.isArray(DATA.standings)&&DATA.standings.length)return DATA.standings;const rows={},M=DATA.matches||[];M.forEach(m=>{const g=cleanGroup(m.home?.group||m.away?.group||(/^Group/i.test(m.stage||'')?m.stage:''));if(!g)return;const h=ensureRow(rows,m.home,g),a=ensureRow(rows,m.away,g);const sh=m.score?.home,sa=m.score?.away;const live=m.status==='LIVE';if(h&&a&&(m.status==='FINISHED'||m.status==='LIVE')&&Number.isFinite(Number(sh))&&Number.isFinite(Number(sa))){addResult(h,Number(sh),Number(sa),live,m.kickoff||'');addResult(a,Number(sa),Number(sh),live,m.kickoff||'')}});Object.values(rows).forEach(r=>{r.results.sort((a,b)=>String(a[0]).localeCompare(String(b[0])));r.form=r.results.slice(-5).map(x=>x[1]).join(' ')});const by={};Object.values(rows).forEach(r=>{if(!r.group)return;(by[r.group] ||= []).push(r)});return Object.keys(by).sort((a,b)=>groupLetter(a).localeCompare(groupLetter(b))).map(g=>{by[g].sort((x,y)=>(y.pts-x.pts)||(y.gd-x.gd)||(y.gf-x.gf)||String(x.name).localeCompare(y.name));by[g].forEach((r,i)=>r.pos=i+1);return {group:g,teams:by[g]}})}
function getThirdRace(){let third=Array.isArray(DATA.third_race)&&DATA.third_race.length?DATA.third_race.map(x=>({...x})):deriveStandings().flatMap(g=>(g.teams||[]).filter(t=>t.pos===3).map(t=>({team:t.name,name:t.name,code:t.code,group:g.group,pts:t.pts,gd:t.gd,gf:t.gf,live:t.live})));third.sort((a,b)=>(b.pts-a.pts)||(b.gd-a.gd)||(b.gf-a.gf)||String(a.team||a.name).localeCompare(String(b.team||b.name)));third.forEach((t,i)=>{t.in=i<8;t.team=t.team||t.name});return third}
function getProjectedSlots(){const existing=DATA.projected_bracket?.slots||[];if(existing.length)return existing;const slots=[];deriveStandings().forEach(g=>{const gl=groupLetter(g.group);(g.teams||[]).forEach(t=>{if(t.pos===1||t.pos===2)slots.push({slot:`${gl}${t.pos}`,team:t.name,code:t.code,pts:t.pts,gd:t.gd,live:t.live})})});getThirdRace().slice(0,8).forEach((t,i)=>slots.push({slot:`3rd #${i+1}`,team:t.team,code:t.code,pts:t.pts,gd:t.gd,live:t.live}));return slots}
function sourceName(a){return a.source||a.feed||'News'}
function renderGroups(){const st=deriveStandings(),host=$('#view-groups');if(!st.length){host.innerHTML=`<div class="vhead">Groups</div><div class="empty">No group data found yet.</div>`;return}host.innerHTML=`<div class="vhead">Groups</div>`+st.map(g=>`<div class="tablewrap"><div class="groupHead">${esc(g.group)}<span>Top 2 · 3rd</span></div><table class="gtable"><thead><tr><th>Team</th><th>P</th><th>W</th><th>D</th><th>L</th><th>GF</th><th>GA</th><th>GD</th><th>Pts</th><th>Form</th></tr></thead><tbody>${(g.teams||[]).map(t=>`<tr class="${t.pos<=2?'qual':t.pos===3?'third':''}"><td><div class="gteam"><span class="pos">${t.pos||''}</span><span class="code">${esc(t.code||'')}</span>${esc(t.name)} ${t.live?'<span class="liveMark">*</span>':''}</div></td><td>${t.pld}</td><td>${t.w}</td><td>${t.d}</td><td>${t.l}</td><td>${t.gf}</td><td>${t.ga}</td><td>${t.gd}</td><td><b>${t.pts}</b></td><td class="form">${esc(t.form||'')}</td></tr>`).join('')}</tbody></table></div>`).join('')}
function bracketTeam(name,code,slot,score,win,live){const nm=name||'TBD';return `<div class="bmTeam ${win?'win':''} ${live?'live':''}"><div class="bmName">${slot?`<span class="bmSlot">${esc(slot)}</span>`:''}${nm==='TBD'?'<span class="bmTbd">TBD</span>':`${esc(code||'')} ${esc(nm)}`}</div><div class="bmScore">${score!=null?esc(score):''}</div></div>`}
function bracketMatch(km,ri,mi,last=false){const live=km.status==='LIVE',done=km.status==='FINISHED';const hs=km.score?.home,as=km.score?.away;const hw=done&&Number(hs)>Number(as),aw=done&&Number(as)>Number(hs);return `<div class="bracketMatch ${last?'':'hasNext'}"><div class="bmMeta"><span>${esc(km.stage||km.round||`Match ${mi+1}`)}</span><span>${live?'LIVE':done?'FT':km.kickoff?dt(km.kickoff):'TBD'}</span></div>${bracketTeam(km.home,km.home_code,'',done||live?hs:null,hw,live)}${bracketTeam(km.away,km.away_code,'',done||live?as:null,aw,live)}</div>`}
function projectedRounds(){const slots=getProjectedSlots();const field=[];for(let i=0;i<32;i++)field.push(slots[i]||{slot:`R32 ${i+1}`,team:'TBD',code:'',pts:0,gd:0});const pairs=[];for(let i=0;i<32;i+=2)pairs.push({stage:`Projected slot ${i/2+1}`,home:field[i].team,home_code:field[i].code,home_slot:field[i].slot,away:field[i+1].team,away_code:field[i+1].code,away_slot:field[i+1].slot,status:'PROJECTED'});return [{round:'Round of 32 field',matches:pairs},{round:'Round of 16',matches:Array.from({length:8},(_,i)=>({stage:`R16 ${i+1}`,home:'TBD',away:'TBD',status:'TBD'}))},{round:'Quarter-finals',matches:Array.from({length:4},(_,i)=>({stage:`QF ${i+1}`,home:'TBD',away:'TBD',status:'TBD'}))},{round:'Semi-finals',matches:Array.from({length:2},(_,i)=>({stage:`SF ${i+1}`,home:'TBD',away:'TBD',status:'TBD'}))},{round:'Final',matches:[{stage:'Final',home:'TBD',away:'TBD',status:'TBD'}]}]}
function projectedMatch(km,ri,mi,last=false){return `<div class="bracketMatch ${last?'':'hasNext'}"><div class="bmMeta"><span>${esc(km.stage||'Projected')}</span><span>${ri===0?'field':'path'}</span></div>${bracketTeam(km.home,km.home_code,km.home_slot,null,false,false)}${bracketTeam(km.away,km.away_code,km.away_slot,null,false,false)}</div>`}
function renderBracket(){const host=$('#view-bracket'),br=DATA.bracket||[];const official=Array.isArray(br)&&br.length;const rounds=official?br:projectedRounds();let html=`<div class="bracketStageHeader"><div class="vhead">Tournament bracket</div><div class="bracketLegend">${official?'Official':'Projected'}</div></div>`;html+=official?'':`<div class="projectedRibbon"><b>${getProjectedSlots().length}</b> qualifier slots</div>`;html+=`<div class="bracketShell"><div class="bracketInner">`+rounds.map((r,ri)=>{const gap=ri===0?10:ri===1?36:ri===2?92:ri===3?210:450;const last=ri===rounds.length-1;return `<section class="bracketRound"><div class="roundTitle">${esc(r.round)}</div><div class="roundStack" style="--rgap:${gap}px">${(r.matches||[]).map((km,mi)=>official?bracketMatch(km,ri,mi,last):projectedMatch(km,ri,mi,last)).join('')}</div></section>`}).join('')+`</div></div>`;host.innerHTML=html}
function renderThird(){const host=$('#view-third'),third=getThirdRace();if(!third.length){host.innerHTML=`<div class="vhead">Third-place tracker</div><div class="empty">Third-place race not available yet.</div>`;return}const cut=third[7];host.innerHTML=`<div class="vhead">Third-place tracker</div><div class="thirdList"><div class="thirdHead"><span>Rank</span><span>Team</span><span>Group</span><span>Pts</span><span>GD</span><span>Status</span></div>${third.map((t,i)=>`<div class="thirdRow ${t.in?'in':'out'}"><div class="thirdRank">#${i+1}</div><div class="thirdTeam"><div class="name">${esc(t.code||'')} ${esc(t.team||'')} ${t.live?'<span class="liveMark">*</span>':''}</div><div class="group">${esc(t.group||'')} · GF ${t.gf??0}</div></div><div class="thirdNum">${esc(t.group||'')}</div><div class="thirdNum pts">${t.pts}</div><div class="thirdNum gd">${t.gd>0?'+':''}${t.gd}</div><div class="thirdBadge ${t.in?'in':''}">${t.in?'IN':'CHASE'}</div></div>`).join('')}<div class="thirdCut">Cut line: ${cut?`${esc(cut.team||cut.name)} at ${cut.pts} pts, GD ${cut.gd>0?'+':''}${cut.gd}`:'waiting for enough teams'}.</div></div>`}
function renderTitle(){const t=DATA.title_odds||[],host=$('#view-title');if(!t.length){host.innerHTML=`<div class="vhead">Odds board</div><div class="empty">Title odds not available yet.</div>`;return}const max=t[0].pct||1;host.innerHTML=`<div class="vhead">Odds board</div><div class="seclbl">Odds to win the tournament · ${t.length} teams</div>`+t.map((x,i)=>`<div class="trow"><span class="trk">${i+1}</span><span class="ttm">${esc(x.team)}</span><span class="tbarwrap"><span class="tbar" style="width:${Math.max(4,Math.round((x.pct||0)/max*100))}%"></span></span><span class="tpct">${x.pct}%</span></div>`).join('')}
function renderEdge(){const host=$('#view-edge');const ups=(DATA.matches||[]).filter(m=>m.prediction);ups.sort((a,b)=>Math.abs((b.prediction?.edge||0))-Math.abs((a.prediction?.edge||0)));if(!ups.length){host.innerHTML=`<div class="vhead">Model</div><div class="empty">No model picks yet.</div>`;return}let html=`<div class="vhead">Model picks</div>`;html+=ups.map((m,i)=>{const pr=m.prediction,big=pr.edge!=null&&Math.abs(pr.edge)>=6,dir=pr.edge>0?'value':pr.edge<0?'fade':'level';return `<div class="ecard ${big?'big':''}"><div class="etop"><span class="estage">${esc(m.stage||'')}</span>${i===0&&big?`<span class="ebest">Top edge</span>`:''}<span class="etag ${dir}">${dir==='value'?'VALUE':dir==='fade'?'CAUTION':'MODEL'}</span></div><div class="ematch">${esc(m.home.name)} <span class="evs">v</span> ${esc(m.away.name)}</div><div class="epick"><span class="epl">Model pick</span><span class="epn">${esc(pr.pick_name)}</span><span class="epc">${pr.confidence}%</span>${pr.edge!=null?`<span class="eedge ${dir}">${pr.edge>0?'+':''}${pr.edge} vs market</span>`:''}</div><div class="ebreak">${esc(edgeBreakdown(m))}</div></div>`}).join('');html+=`<div class="edisc">Model output uses football-data standings/form plus The Odds API market data when available. It is not a guarantee.</div>`;host.innerHTML=html}
function newsSources(){return ['all',...[...new Set((DATA.news||[]).map(n=>sourceName(n)))].sort()]}
function renderNews(){const n=DATA.news||[],host=$('#view-news'),links=DATA.social_links||[],diag=DATA.diagnostics||[];const srcs=newsSources();let list=NEWS_FILTER==='all'?n:n.filter(a=>sourceName(a)===NEWS_FILTER);host.innerHTML=`<div class="vhead">News cycle</div><div class="banner"><b>Multi-source feed.</b> Every card now shows its source. Public RSS/Google News feeds are mixed with ESPN; X is quick links only, not scraped.</div>${links.length?`<div class="seclbl">Social / source quick links</div><div class="socialGrid">${links.map(l=>`<a href="${esc(l.url)}" target="_blank" rel="noopener">${esc(l.name)}</a>`).join('')}</div>`:''}<div class="srcCount">${n.length} headlines · ${srcs.length-1} visible sources</div><div class="newsTools">${srcs.map(s=>`<button class="chip ${NEWS_FILTER===s?'on':''}" data-src="${esc(s)}" onclick="NEWS_FILTER=this.dataset.src;renderNews()">${esc(s)}</button>`).join('')}</div>`+(list.length?list.map(a=>`<a class="ncard" href="${esc(a.link||'#')}" target="_blank" rel="noopener"><div class="srcTop"><span class="srcBadge">${esc(sourceName(a))}</span>${a.feed&&a.feed!==sourceName(a)?`<span class="feedBadge">via ${esc(a.feed)}</span>`:''}</div><div class="nhead">${esc(a.headline)}</div>${a.desc?`<div class="ndesc">${esc(a.desc)}</div>`:''}<div class="nmeta">${a.published?ago(a.published):''}</div></a>`).join(''):`<div class="empty">No headlines for this source right now. Run <code>python fetch_data.py</code> once to refresh feeds.</div>`)+(diag.length?`<div class="diagList">${diag.filter(d=>String(d).toLowerCase().includes('news')).map(d=>`<div>${esc(d)}</div>`).join('')}</div>`:'')}

const SYSTEM_UPDATES=[
  {date:'Current',tag:'UI',title:'System updates hub',items:['Updates tab added.','Main screens cleaned.']},
  {date:'Recent',tag:'UI',title:'Model command center',items:[
    'Reworked the Model tab into compact KPIs, filter chips, a best-read spotlight, and a scan-friendly pick board.',
    'Clicking a model row still opens the larger match view with the original model and odds fields.'
  ]},
  {date:'Recent',tag:'UI',title:'Fixture spotlight modal',items:[
    'Match cards open in a larger modal instead of expanding downward and creating empty vertical space.',
    'The modal groups model pick, odds, match read, stats, and lineups into a more readable layout.'
  ]},
  {date:'Recent',tag:'UI',title:'Bracket, thirds, and live-strip polish',items:[
    'Bracket was redesigned into a smaller two-sided tournament view.',
    'Third-place tracker was changed into a top-to-bottom ranking table.',
    'Top strip text spacing and past-match hiding were cleaned up.'
  ]},
  {date:'Recent',tag:'Feeds',title:'News UI fixes',items:[
    'News cards preserve source labels better and the all-sources view is designed to avoid one feed visually dominating.',
    'This UI tab does not fetch news itself; it only displays what your existing data.json provides.'
  ]}
];
function markUpdatesRead(){localStorage.setItem('matchday.updates.lastSeen',new Date().toISOString());renderSystemUpdates()}
function renderSystemUpdates(){const host=$('#view-updates');const seen=localStorage.getItem('matchday.updates.lastSeen');const latest='UI build';host.innerHTML=`<div class="updatesShell"><div class="updatesHero"><section class="updatesIntro"><h2>System updates</h2><span class="safePill">UI</span></section><aside class="buildCard"><div class="tiny">Current build</div><div class="build">${esc(latest)}</div><div class="hint">Last viewed: ${seen?esc(ago(seen)):'not marked yet'}</div><div class="updateActions"><button class="miniBtn" onclick="markUpdatesRead()">Mark as read</button><button class="miniBtn" onclick="setView('status')">Open Status</button></div></aside></div><section class="timeline"><div class="timelineHead"><h3>Release notes</h3><span>${SYSTEM_UPDATES.length} entries</span></div>${SYSTEM_UPDATES.map(u=>`<article class="updateItem"><div class="updateDate">${esc(u.date)}</div><div><div class="updateTitle"><span>${esc(u.title)}</span><span class="updateBadge">${esc(u.tag)}</span></div><ul>${u.items.map(i=>`<li>${esc(i)}</li>`).join('')}</ul></div></article>`).join('')}</section></div>`}

function renderStatus(){const host=$('#view-status'),M=DATA.matches||[],st=deriveStandings(),third=getThirdRace();const live=M.filter(m=>m.status==='LIVE').length,up=M.filter(m=>m.status==='UPCOMING').length,fin=M.filter(m=>m.status==='FINISHED').length;const next=M.filter(isVisibleUpcoming).sort((a,b)=>(a.kickoff||'').localeCompare(b.kickoff||''))[0];host.innerHTML=`<div class="vhead">App Status</div><div class="status-grid"><div class="statuscard ${LAST_OK?'ok':'warn'}"><span class="slbl">Data file</span><div class="sval">${LAST_OK?'loaded':'not loaded'}</div><div class="hint">${LAST_ERROR?esc(LAST_ERROR):'Loaded'}</div></div><div class="statuscard info"><span class="slbl">Source</span><div class="sval">${esc(DATA.source_note||'unknown')}</div><div class="hint">${esc(DATA.standings_mode||'')}</div></div><div class="statuscard info"><span class="slbl">Updated</span><div class="sval">${DATA.updated?ago(DATA.updated):'unknown'}</div><div class="hint">${esc(DATA.updated||'—')}</div></div><div class="statuscard info"><span class="slbl">Matches</span><div class="sval">${M.length}</div><div class="hint">${live} live · ${up} upcoming · ${fin} finished</div></div><div class="statuscard info"><span class="slbl">Groups</span><div class="sval">${st.length}</div><div class="hint">${third.length} third-place teams tracked</div></div><div class="statuscard info"><span class="slbl">News Items</span><div class="sval">${(DATA.news||[]).length}</div><div class="hint">${newsSources().filter(s=>s!=='all').join(' · ')}</div></div></div><div class="btnline"><button class="actionbtn" onclick="load(true)">Reload Data Now</button><button class="actionbtn" onclick="setView('groups')">Open Groups</button><button class="actionbtn" onclick="setView('third')">Open Thirds</button><button class="actionbtn" onclick="setView('updates')">System Updates</button></div>`}
function opt(v,label,cur){return `<option value="${v}" ${String(cur)===String(v)?'selected':''}>${label}</option>`}function checked(v){return v?'checked':''}
function renderCustomize(){const host=$('#view-customize');host.innerHTML=`<div class="vhead">Customize</div><div class="banner"><b>Personalize your terminal.</b> These settings save locally in this browser/app window.</div><div class="settings-grid"><div class="setcard"><label>Accent color</label><select onchange="updateSetting('accent',this.value)">${opt('orange','Matchday orange',SETTINGS.accent)}${opt('blue','Electric blue',SETTINGS.accent)}${opt('green','Pitch green',SETTINGS.accent)}${opt('red','Live red',SETTINGS.accent)}${opt('purple','Night purple',SETTINGS.accent)}</select><div class="hint">Changes highlights, buttons and the brand dot.</div></div><div class="setcard"><label>Card density</label><select onchange="updateSetting('density',this.value)">${opt('compact','Compact',SETTINGS.density)}${opt('normal','Normal',SETTINGS.density)}${opt('spacious','Spacious',SETTINGS.density)}</select><div class="hint">Compact fits more matches; spacious feels more premium.</div></div><div class="setcard"><label>Panel style</label><select onchange="updateSetting('panel',this.value)">${opt('glass','Soft glass',SETTINGS.panel)}${opt('flat','Flat dark',SETTINGS.panel)}</select><div class="hint">Flat mode is lighter on older laptops.</div></div><div class="setcard"><label>Default tab</label><select onchange="updateSetting('defaultView',this.value)">${['matches','groups','title','edge','bracket','third','news','status','updates'].map(v=>opt(v,v[0].toUpperCase()+v.slice(1),SETTINGS.defaultView)).join('')}</select><div class="hint">Selected when the page starts.</div></div><div class="setcard"><label>Refresh rate</label><select onchange="updateSetting('refresh',this.value)">${opt(30,'Every 30 seconds',SETTINGS.refresh)}${opt(60,'Every 60 seconds',SETTINGS.refresh)}${opt(180,'Every 3 minutes',SETTINGS.refresh)}${opt(300,'Every 5 minutes',SETTINGS.refresh)}</select><div class="hint">This controls the dashboard reload from data.json.</div></div><div class="setcard"><label>Display</label><div class="switchrow"><span>Right insight panel</span><input type="checkbox" ${checked(SETTINGS.showInsight)} onchange="updateSetting('showInsight',this.checked)"></div><div class="switchrow" style="margin-top:10px"><span>Match detail panels</span><input type="checkbox" ${checked(SETTINGS.showDetails)} onchange="updateSetting('showDetails',this.checked)"></div></div></div><div class="btnline"><button class="actionbtn" onclick="resetSettings()">Reset settings</button><button class="actionbtn" onclick="setView('status')">Check app status</button></div>`}
function renderCurrent(){({matches:renderMatches,groups:renderGroups,title:renderTitle,edge:renderEdge,bracket:renderBracket,third:renderThird,news:renderNews,status:renderStatus,updates:renderSystemUpdates,customize:renderCustomize}[VIEW]||renderMatches)()}
function renderStrip(){const M=DATA.matches||[],live=M.filter(m=>m.status==='LIVE'),next=M.filter(isVisibleUpcoming).sort((a,b)=>(a.kickoff||'').localeCompare(b.kickoff||''))[0];const parts=[];if(live.length){const m=live[0];parts.push(`<span class="ls-tag"><span class="dot"></span>LIVE${m.minute?` ${m.minute}'`:''}</span>`);parts.push(`<span class="ls-live">${esc(m.home.code)} ${m.score?.home??0}–${m.score?.away??0} ${esc(m.away.code)}</span>`);if(live.length>1)parts.push(`<span class="ls-next">+${live.length-1} more live</span>`)}else parts.push(`<span class="ls-next">No live matches</span>`);if(next)parts.push(`<span class="ls-next">Next · <b>${esc(next.home.code)} v ${esc(next.away.code)}</b> ${kickIn(next.kickoff)}</span>`);else parts.push(`<span class="ls-next">No upcoming fixtures</span>`);parts.push(`<span class="ls-upd">Updated ${ago(DATA.updated)}</span>`);$('#strip').innerHTML=parts.join('')}
function diverseNews(limit=6){const n=DATA.news||[];const buckets={};n.forEach(a=>{const src=sourceName(a);(buckets[src] ||= []).push(a)});const srcs=Object.keys(buckets).sort();const out=[];let i=0;while(out.length<limit&&srcs.length){let added=false;for(const src of srcs){const item=buckets[src][i];if(item){out.push(item);added=true;if(out.length>=limit)break}}if(!added)break;i++}return out.length?out:n.slice(0,limit)}
function renderInsight(){const host=$('#insight'),M=DATA.matches||[];const focus=M.find(m=>m.status==='LIVE')||M.filter(isVisibleUpcoming).sort((a,b)=>(a.kickoff||'').localeCompare(b.kickoff||''))[0]||M.find(m=>m.status==='FINISHED')||M[0];let h=`<div class="seclbl">In focus</div>`;if(focus){h+=`<div class="ins-match">${esc(focus.home.name)} <span class="evs">v</span> ${esc(focus.away.name)}</div><div class="ins-sub">${focus.status==='LIVE'?`LIVE ${focus.minute||''}' · ${focus.score?.home??0}–${focus.score?.away??0}`:`${esc(focus.stage||'')} · ${kickIn(focus.kickoff)}`}</div>`;h+=modelBlock(focus);const x=(focus.markets||{})['1x2']||{};if(x.home_pct!=null)h+=`<div class="prob" style="margin-top:12px"><div class="problbl"><span>${esc(focus.home.code)}</span><span>draw</span><span>${esc(focus.away.code)}</span></div>${bar1x2(x.home_pct,x.draw_pct,x.away_pct)}</div>`;const bd=edgeBreakdown(focus);if(bd)h+=`<div class="seclbl" style="margin-top:16px">Model read</div><div class="ins-summary"><p>${esc(bd)}</p></div>`}else h+=`<div class="faintline">No match in focus yet.</div>`;const n=diverseNews(6);if(n.length)h+=`<div class="seclbl" style="margin-top:18px">Latest from multiple sources</div>`+n.map(a=>`<a class="ins-news" href="${esc(a.link||'#')}" target="_blank" rel="noopener"><span class="insSource">${esc(sourceName(a))}</span><br>${esc(a.headline)}</a>`).join('');host.innerHTML=h}
function setView(v){VIEW=v;document.querySelectorAll('.navbtn').forEach(b=>b.setAttribute('aria-pressed',b.dataset.v===v));document.querySelectorAll('.view').forEach(el=>el.style.display=el.id==='view-'+v?(v==='matches'?'grid':'block'):'none');renderCurrent()}
$('#nav').addEventListener('click',e=>{const b=e.target.closest('.navbtn');if(b)setView(b.dataset.v)});
async function load(manual=false){if(LOAD_TIMER){clearTimeout(LOAD_TIMER);LOAD_TIMER=null}try{const r=await fetch('data.json?_='+Date.now());if(!r.ok)throw new Error('HTTP '+r.status);DATA=await r.json();BYID={};(DATA.matches||[]).forEach(m=>BYID[m.id]=m);LAST_OK=true;LAST_ERROR='';const note=(DATA.source_note||'').toLowerCase();$('#banner').innerHTML=note.includes('sample')?``:'';renderStrip();renderInsight();renderCurrent()}catch(e){console.error(e);LAST_OK=false;LAST_ERROR=String(e.message||e);$('#strip').textContent='no data';$('#view-matches').innerHTML=`<div class="empty" style="grid-column:1/-1">Data file not loaded.<br><span class="faintline">${esc(LAST_ERROR)}</span></div>`;if(VIEW==='status')renderStatus()}finally{scheduleNextLoad()}}
function scheduleNextLoad(){if(LOAD_TIMER)clearTimeout(LOAD_TIMER);LOAD_TIMER=setTimeout(()=>load(),Math.max(30,Number(SETTINGS.refresh)||60)*1000)}


/* ===== UI PATCH: keep data untouched; improve news source logic and match opening ===== */
function _srcClean(x){x=String(x||'').replace(/\s+/g,' ').trim();if(!x)return'';x=x.replace(/^www\./i,'').replace(/\.com$/i,'');const map=[[/espn/i,'ESPN'],[/bbc/i,'BBC Sport'],[/guardian/i,'The Guardian'],[/sky\s*sports|skysports/i,'Sky Sports'],[/cbs/i,'CBS Sports'],[/le\s*monde|lemonde/i,'Le Monde'],[/fox\s*sports|foxsports/i,'FOX Sports'],[/reuters/i,'Reuters'],[/associated\s*press|\bap\b/i,'Associated Press'],[/fifa/i,'FIFA'],[/google\s*news/i,'Google News']];for(const [re,n] of map)if(re.test(x))return n;return x.split('|')[0].trim()}
function _srcFromTitle(a){const h=String(a.headline||a.title||'');const parts=h.split(/\s[-–—]\s/g).map(x=>x.trim()).filter(Boolean);if(parts.length>1){const tail=parts[parts.length-1];if(tail.length<=35&&!/world cup|football|soccer|latest|news/i.test(tail))return _srcClean(tail)}return''}
function _srcFromLink(a){try{const u=new URL(a.link||a.url||'',location.href);let h=u.hostname.replace(/^www\./,'');if(h.includes('news.google.'))return'';if(h.includes('espn'))return'ESPN';if(h.includes('bbc'))return'BBC Sport';if(h.includes('theguardian'))return'The Guardian';if(h.includes('skysports'))return'Sky Sports';if(h.includes('cbssports'))return'CBS Sports';if(h.includes('lemonde'))return'Le Monde';if(h.includes('foxsports'))return'FOX Sports';if(h.includes('reuters'))return'Reuters';if(h.includes('fifa'))return'FIFA';return _srcClean(h.split('.')[0])}catch(e){return''}}
function sourceName(a){const titleSrc=_srcFromTitle(a),linkSrc=_srcFromLink(a);const raw=_srcClean(a.source_name||a.publisher||a.provider||a.source||'');const feed=_srcClean(a.feed||'');const generic=s=>!s||/^(news|headlines|football|soccer|rss|google news)$/i.test(s);if(titleSrc&&!generic(titleSrc))return titleSrc;if(linkSrc&&!generic(linkSrc))return linkSrc;if(raw&&!generic(raw))return raw;if(feed&&!generic(feed))return feed;return raw||feed||'News'}
function feedName(a){const f=_srcClean(a.feed||a.source||'');return f&&f!==sourceName(a)?f:''}
function newsBuckets(){const buckets={};(DATA.news||[]).forEach((a,i)=>{const src=sourceName(a);(buckets[src] ||= []).push({...a,_idx:i})});return buckets}
function newsSources(){const buckets=newsBuckets();return ['all',...Object.keys(buckets).sort((a,b)=>a.localeCompare(b))]}
function diverseNews(limit=8){const buckets=newsBuckets(),srcs=Object.keys(buckets).sort((a,b)=>a.localeCompare(b));const out=[];let row=0;while(out.length<limit&&srcs.length){let moved=false;for(const src of srcs){const item=buckets[src][row];if(item){out.push(item);moved=true;if(out.length>=limit)break}}if(!moved)break;row++}return out.length?out:(DATA.news||[]).slice(0,limit)}
function renderNews(){const n=DATA.news||[],host=$('#view-news'),links=DATA.social_links||[],diag=DATA.diagnostics||[];const buckets=newsBuckets(),srcs=newsSources();if(NEWS_FILTER!=='all'&&!buckets[NEWS_FILTER])NEWS_FILTER='all';let list=NEWS_FILTER==='all'?diverseNews(n.length):buckets[NEWS_FILTER]||[];host.innerHTML=`<div class="vhead">News cycle</div><div class="banner"><b>Balanced multi-source feed.</b> The All view is now interleaved by outlet, so one source cannot dominate the page. Source labels are inferred from publisher fields, title suffixes, and link domains.</div>${links.length?`<div class="seclbl">Social / source quick links</div><div class="socialGrid">${links.map(l=>`<a href="${esc(l.url)}" target="_blank" rel="noopener">${esc(l.name)}</a>`).join('')}</div>`:''}<div class="srcCount">${n.length} headlines · ${srcs.length-1} detected sources</div><div class="newsTools">${srcs.map(s=>`<button class="chip ${s==='all'?'allchip':''} ${NEWS_FILTER===s?'on':''}" data-src="${esc(s)}" onclick="NEWS_FILTER=this.dataset.src;renderNews()">${esc(s==='all'?'All sources':s)}<span class="count">${s==='all'?n.length:(buckets[s]||[]).length}</span></button>`).join('')}</div>`+(list.length?list.map(a=>`<a class="ncard" href="${esc(a.link||a.url||'#')}" target="_blank" rel="noopener"><div class="srcTop"><span class="srcBadge">${esc(sourceName(a))}</span>${feedName(a)?`<span class="feedBadge">via ${esc(feedName(a))}</span>`:''}</div><div class="nhead">${esc(a.headline||a.title||'Untitled')}</div>${a.desc||a.description?`<div class="ndesc">${esc(a.desc||a.description)}</div>`:''}<div class="nmeta">${a.published?ago(a.published):''}</div></a>`).join(''):`<div class="empty">No headlines for this source right now. Run <code>python fetch_data.py</code> once to refresh feeds.</div>`)+(diag.length?`<div class="diagList">${diag.filter(d=>String(d).toLowerCase().includes('news')).map(d=>`<div>${esc(d)}</div>`).join('')}</div>`:'')}
function renderInsight(){const host=$('#insight'),M=DATA.matches||[];const focus=M.find(m=>m.status==='LIVE')||M.filter(isVisibleUpcoming).sort((a,b)=>(a.kickoff||'').localeCompare(b.kickoff||''))[0]||M.find(m=>m.status==='FINISHED')||M[0];let h=`<div class="seclbl">In focus</div>`;if(focus){h+=`<div class="ins-match">${esc(focus.home.name)} <span class="evs">v</span> ${esc(focus.away.name)}</div><div class="ins-sub">${focus.status==='LIVE'?`LIVE ${focus.minute||''}' · ${focus.score?.home??0}–${focus.score?.away??0}`:`${esc(focus.stage||'')} · ${kickIn(focus.kickoff)}`}</div>`;h+=modelBlock(focus);const x=(focus.markets||{})['1x2']||{};if(x.home_pct!=null)h+=`<div class="prob" style="margin-top:12px"><div class="problbl"><span>${esc(focus.home.code)}</span><span>draw</span><span>${esc(focus.away.code)}</span></div>${bar1x2(x.home_pct,x.draw_pct,x.away_pct)}</div>`;const bd=edgeBreakdown(focus);if(bd)h+=`<div class="seclbl" style="margin-top:16px">Model read</div><div class="ins-summary"><p>${esc(bd)}</p></div>`}else h+=`<div class="faintline">No match in focus yet.</div>`;const n=diverseNews(6);if(n.length)h+=`<div class="seclbl" style="margin-top:18px">Latest from multiple sources</div>`+n.map(a=>`<a class="ins-news" href="${esc(a.link||a.url||'#')}" target="_blank" rel="noopener"><span class="insSource">${esc(sourceName(a))}</span><br>${esc(a.headline||a.title||'Untitled')}</a>`).join('');host.innerHTML=h}
function ensureMatchModal(){let modal=document.getElementById('matchModal');if(modal)return modal;modal=document.createElement('div');modal.id='matchModal';modal.className='matchModal';modal.addEventListener('click',e=>{if(e.target===modal)closeMatchModal()});document.body.appendChild(modal);return modal}
function openMatchModal(id){const m=BYID[id]||(DATA.matches||[]).find(x=>String(x.id)===String(id));if(!m)return;const modal=ensureMatchModal();const hmeta=t=>`${t?.pos?`#${t.pos} · `:''}${t?.pts??0} pts${t?.form?` · ${esc(t.form)}`:''}`;modal.innerHTML=`<section class="matchSheet" role="dialog" aria-modal="true"><div class="modalHero"><button class="modalClose" onclick="closeMatchModal()" aria-label="Close">×</button><div class="modalStage">${esc(m.stage||'Fixture')} · ${esc(m.status||'')}</div><div class="modalFixture"><div class="modalTeam"><div class="modalCode">${esc(m.home?.code||'HOME')}</div><div class="modalName">${esc(m.home?.name||'Home')}</div><div class="modalMeta">${hmeta(m.home)}</div></div><div class="modalScore"><div class="bigScore">${scoreText(m).replace(/<[^>]+>/g,'')}</div><div class="modalStatus">${m.status==='LIVE'?`LIVE ${m.minute||''}'`:kickIn(m.kickoff)}</div></div><div class="modalTeam away"><div class="modalCode">${esc(m.away?.code||'AWAY')}</div><div class="modalName">${esc(m.away?.name||'Away')}</div><div class="modalMeta">${hmeta(m.away)}</div></div></div></div><div class="modalBody">${details(m)}</div></section>`;modal.classList.add('show');document.body.classList.add('modalOpen')}
function closeMatchModal(){const modal=document.getElementById('matchModal');if(modal)modal.classList.remove('show');document.body.classList.remove('modalOpen')}
document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMatchModal()});
function cardHTML(m){const live=m.status==='LIVE',stale=isStaleUpcoming(m),displayStatus=stale?'PAST / REFRESH':m.status;const x=(m.markets&&m.markets['1x2'])||{};const probTop=x.home_pct!=null?`<div class="prob"><div class="problbl"><span>${esc(m.home.code||m.home.name)}</span><span>Market read</span><span>${esc(m.away.code||m.away.name)}</span></div>${bar1x2(x.home_pct,x.draw_pct,x.away_pct)}</div>`:`<div class="prob"><div class="nomk">No market odds yet</div></div>`;const pr=m.prediction;const pick=pr?`<div class="pick ${pr.edge!=null&&Math.abs(pr.edge)>=6?'edge':''}"><span class="pl">Pick</span><span class="pn">${esc(pr.pick_name)}</span><span class="pc">${pr.confidence}%</span><span class="pnote">${esc(pr.note||'')}</span></div>`:'';return `<article class="card" data-id="${esc(m.id)}"><div class="head" onclick="openMatchModal(this.closest('article').dataset.id)"><div class="metarow"><span class="stage">${esc(m.stage||'Fixture')}</span><span class="spacer"></span><span class="pill ${esc(displayStatus)}">${live?'<span class="blink"></span>':''}${esc(displayStatus)}</span></div><div class="fixture"><div class="side"><div class="tname">${esc(m.home.name)}</div><div class="tsub"><span>${esc(m.home.code)}</span><span>${m.home.pos?`#${m.home.pos}`:''}</span><span>${m.home.pts??0} pts</span></div></div><div class="center"><div class="score">${scoreText(m)}</div>${live?`<div class="minute">${m.minute||''}'</div>`:`<div class="kick">${stale?'past kickoff':kickIn(m.kickoff)}</div>`}</div><div class="side away"><div class="tname">${esc(m.away.name)}</div><div class="tsub"><span>${esc(m.away.code)}</span><span>${m.away.pos?`#${m.away.pos}`:''}</span><span>${m.away.pts??0} pts</span></div></div></div>${probTop}${pick}<div class="expander"></div></div></article>`}



/* ===== PATCH: better source diversity + compact two-sided bracket ===== */
const _ISO3={AFG:'AF',ALB:'AL',ALG:'DZ',AND:'AD',ANG:'AO',ARG:'AR',ARM:'AM',AUS:'AU',AUT:'AT',AZE:'AZ',BAH:'BS',BHR:'BH',BAN:'BD',BEL:'BE',BIH:'BA',BOL:'BO',BRA:'BR',BUL:'BG',CAM:'CM',CAN:'CA',CHI:'CL',CHN:'CN',COL:'CO',CRC:'CR',CRO:'HR',CUB:'CU',CZE:'CZ',DEN:'DK',DOM:'DO',ECU:'EC',EGY:'EG',ENG:'GB',ESP:'ES',FIN:'FI',FRA:'FR',GER:'DE',GHA:'GH',GRE:'GR',GUA:'GT',HAI:'HT',HON:'HN',HUN:'HU',IND:'IN',IDN:'ID',IRN:'IR',IRQ:'IQ',IRL:'IE',ISR:'IL',ITA:'IT',JAM:'JM',JPN:'JP',KOR:'KR',KUW:'KW',MAR:'MA',MEX:'MX',NED:'NL',NOR:'NO',NZL:'NZ',PAN:'PA',PAR:'PY',PER:'PE',POL:'PL',POR:'PT',QAT:'QA',ROU:'RO',RSA:'ZA',RUS:'RU',KSA:'SA',SCO:'GB',SEN:'SN',SRB:'RS',SUI:'CH',SVK:'SK',SVN:'SI',SWE:'SE',TUN:'TN',TUR:'TR',UKR:'UA',URU:'UY',USA:'US',VEN:'VE',WAL:'GB',CPV:'CV',CIV:'CI',CUW:'CW'};
function flagEmoji(code){code=String(code||'').toUpperCase().trim();let c=_ISO3[code]||(/^[A-Z]{2}$/.test(code)?code:'');if(!c)return'';return c.replace(/./g,ch=>String.fromCodePoint(127397+ch.charCodeAt(0)))}
function codeForTeam(name,explicit){if(explicit)return explicit;const n=String(name||'').toLowerCase();for(const g of DATA.standings||[])for(const t of g.teams||[])if(String(t.name||'').toLowerCase()===n)return t.code||'';for(const m of DATA.matches||[]){if(String(m.home?.name||'').toLowerCase()===n)return m.home.code||'';if(String(m.away?.name||'').toLowerCase()===n)return m.away.code||''}return''}
function bracketTeam(name,code,slot,score,win,live){const nm=name||'TBD',cd=codeForTeam(nm,code),fl=flagEmoji(cd);return `<div class="bmTeam ${win?'win':''} ${live?'live':''}"><div class="bmName">${slot?`<span class="bmSlot">${esc(slot)}</span>`:''}${fl?`<span class="flag">${fl}</span>`:''}<span class="bmCode">${esc(cd||'')}</span><span>${nm==='TBD'?'<span class="bmTbd">TBD</span>':esc(nm)}</span></div><div class="bmScore">${score!=null?esc(score):''}</div></div>`}
function compactBracketMatch(km,last=false,side='left',isFinal=false){const live=km.status==='LIVE',done=km.status==='FINISHED';const hs=km.score?.home,as=km.score?.away;const hw=done&&Number(hs)>Number(as),aw=done&&Number(as)>Number(hs);const cls=`brMini ${live?'live':''} ${done?'done':''} ${isFinal?'finalCard':''} ${last?'':side==='right'?'connectL':'connectR'} ${(!km.home&&!km.away)?'tbd':''}`;return `<div class="${cls}">${isFinal?'<div class="finalBadge">Final</div>':''}<div class="bmMeta"><span>${esc(km.stage||km.round||'Match')}</span><span>${live?'LIVE':done?'FT':km.kickoff?dt(km.kickoff):'TBD'}</span></div>${bracketTeam(km.home,km.home_code||km.homeCode||'',km.home_slot||'',done||live?hs:null,hw,live)}${bracketTeam(km.away,km.away_code||km.awayCode||'',km.away_slot||'',done||live?as:null,aw,live)}</div>`}
function compactProjectedMatch(km,last=false,side='left',isFinal=false){const cls=`brMini ${isFinal?'finalCard':''} ${last?'':side==='right'?'connectL':'connectR'}`;return `<div class="${cls}">${isFinal?'<div class="finalBadge">Final</div>':''}<div class="bmMeta"><span>${esc(km.stage||'Projected')}</span><span>${esc(km.kickoff?'path':'slot')}</span></div>${bracketTeam(km.home,km.home_code||'',km.home_slot||'',null,false,false)}${bracketTeam(km.away,km.away_code||'',km.away_slot||'',null,false,false)}</div>`}
function splitHalf(arr,half){arr=arr||[];const mid=Math.ceil(arr.length/2);return half==='left'?arr.slice(0,mid):arr.slice(mid)}
function roundMatches(rounds,name){const r=(rounds||[]).find(x=>String(x.round||'').toLowerCase()===name.toLowerCase());return r?.matches||[]}
function shortRoundName(name){return name.replace('Round of ','R').replace('Quarter-finals','QF').replace('Semi-finals','SF').replace('Third-place playoff','3rd')}
function bracketColumn(title,matches,side,official,connect=true){return `<section class="brCol"><div class="roundTitle">${esc(shortRoundName(title))}</div>${matches.length?matches.map(m=>official?compactBracketMatch(m,!connect,side,false):compactProjectedMatch(m,!connect,side,false)).join(''):`<div class="brEmptyPath">waiting</div>`}</section>`}
function renderBracket(){const host=$('#view-bracket'),br=DATA.bracket||[];const official=Array.isArray(br)&&br.length;const rounds=official?br:projectedRounds();const order=['Round of 32','Round of 16','Quarter-finals','Semi-finals'];const finalM=roundMatches(rounds,'Final')[0]||{stage:'Final',home:'TBD',away:'TBD',score:{},status:'UPCOMING'};const thirdM=roundMatches(rounds,'Third-place playoff')[0];let html=`<div class="bracketStageHeader"><div class="vhead">Tournament bracket</div><div class="bracketLegend">${official?'Official':'Projected'}</div></div>`;html+='';html+=`<div class="bracketMiniNote"><span><b>${getProjectedSlots().length}</b> qualifier slots</span></div><div class="bracketShell"><div class="bracketBoard">`;
html+=order.map(r=>bracketColumn(r,splitHalf(roundMatches(rounds,r),'left'),'left',official,true)).join('');
html+=`<section class="brCenter">${official?compactBracketMatch(finalM,true,'center',true):compactProjectedMatch(finalM,true,'center',true)}${thirdM?(official?compactBracketMatch(thirdM,true,'center',false):compactProjectedMatch(thirdM,true,'center',false)):''}</section>`;
html+=order.slice().reverse().map(r=>bracketColumn(r,splitHalf(roundMatches(rounds,r),'right'),'right',official,true)).join('');
html+=`</div></div>`;host.innerHTML=html}

function _srcClean(x){x=String(x||'').replace(/\s+/g,' ').trim();if(!x)return'';x=x.replace(/^www\./i,'').replace(/\.com$/i,'');const map=[[/espn|espn fc/i,'ESPN'],[/bbc/i,'BBC Sport'],[/guardian/i,'The Guardian'],[/sky\s*sports|skysports/i,'Sky Sports'],[/cbs/i,'CBS Sports'],[/fox\s*sports|foxsports/i,'FOX Sports'],[/reuters/i,'Reuters'],[/associated\s*press|^ap$/i,'Associated Press'],[/fifa/i,'FIFA'],[/le\s*monde|lemonde/i,'Le Monde'],[/marca/i,'Marca'],[/goal/i,'Goal'],[/yahoo/i,'Yahoo Sports'],[/nbc/i,'NBC Sports'],[/google\s*news/i,'Google News']];for(const [re,n] of map)if(re.test(x))return n;return x.split('|')[0].trim()}
function _srcFromTitle(a){const h=String(a.headline||a.title||'');const parts=h.split(/\s[-–—]\s/g).map(x=>x.trim()).filter(Boolean);if(parts.length>1){const tail=parts[parts.length-1];if(tail.length<=42&&!/world cup|football|soccer|latest|news|live/i.test(tail))return _srcClean(tail)}return''}
function _srcFromLink(a){try{const u=new URL(a.link||a.url||'',location.href);let h=u.hostname.replace(/^www\./,'');if(h.includes('news.google.'))return'';if(h.includes('espn'))return'ESPN';if(h.includes('bbc'))return'BBC Sport';if(h.includes('theguardian'))return'The Guardian';if(h.includes('skysports'))return'Sky Sports';if(h.includes('cbssports'))return'CBS Sports';if(h.includes('foxsports'))return'FOX Sports';if(h.includes('reuters'))return'Reuters';if(h.includes('apnews'))return'Associated Press';if(h.includes('fifa'))return'FIFA';if(h.includes('marca'))return'Marca';if(h.includes('goal'))return'Goal';return _srcClean(h.split('.')[0])}catch(e){return''}}
function sourceName(a){const raw=_srcClean(a.source_name||a.publisher||a.provider||a.source||a.feed_source||'');const feed=_srcClean(a.feed||'');const titleSrc=_srcFromTitle(a),linkSrc=_srcFromLink(a);const generic=s=>!s||/^(news|headlines|football|soccer|rss|google news|world cup)$/i.test(s);if(raw&&!generic(raw))return raw;if(titleSrc&&!generic(titleSrc))return titleSrc;if(linkSrc&&!generic(linkSrc))return linkSrc;if(feed&&!generic(feed))return feed;return raw||feed||'News'}
function feedName(a){const f=_srcClean(a.feed||'');return f&&f!==sourceName(a)?f:''}
function newsBuckets(){const buckets={};(DATA.news||[]).forEach((a,i)=>{const src=sourceName(a);(buckets[src] ||= []).push({...a,_idx:i})});return buckets}
function newsSources(){const buckets=newsBuckets();return ['all',...Object.keys(buckets).sort((a,b)=>a.localeCompare(b))]}
function diverseNews(limit=12){const buckets=newsBuckets(),srcs=Object.keys(buckets).sort((a,b)=>a.localeCompare(b));const out=[];let row=0;while(out.length<limit&&srcs.length){let moved=false;for(const src of srcs){const item=buckets[src][row];if(item){out.push(item);moved=true;if(out.length>=limit)break}}if(!moved)break;row++}return out.length?out:(DATA.news||[]).slice(0,limit)}
function renderNews(){const n=DATA.news||[],host=$('#view-news'),links=DATA.social_links||[],diag=DATA.diagnostics||[];const buckets=newsBuckets(),srcs=newsSources();if(NEWS_FILTER!=='all'&&!buckets[NEWS_FILTER])NEWS_FILTER='all';let list=NEWS_FILTER==='all'?diverseNews(Math.max(n.length,18)):buckets[NEWS_FILTER]||[];host.innerHTML=`<div class="vhead">News cycle</div>${links.length?`<div class="seclbl">Social / source quick links</div><div class="socialGrid">${links.map(l=>`<a href="${esc(l.url)}" target="_blank" rel="noopener">${esc(l.name)}</a>`).join('')}</div>`:''}<div class="srcCount">${n.length} headlines · ${srcs.length-1} detected sources</div><div class="newsTools">${srcs.map(s=>`<button class="chip ${s==='all'?'allchip':''} ${NEWS_FILTER===s?'on':''}" data-src="${esc(s)}" onclick="NEWS_FILTER=this.dataset.src;renderNews()">${esc(s==='all'?'All sources':s)}<span class="count">${s==='all'?n.length:(buckets[s]||[]).length}</span></button>`).join('')}</div>`+(list.length?list.map(a=>`<a class="ncard" href="${esc(a.link||a.url||'#')}" target="_blank" rel="noopener"><div class="srcTop"><span class="srcBadge">${esc(sourceName(a))}</span>${feedName(a)?`<span class="feedBadge">via ${esc(feedName(a))}</span>`:''}</div><div class="nhead">${esc(a.headline||a.title||'Untitled')}</div>${a.desc||a.description?`<div class="ndesc">${esc(a.desc||a.description)}</div>`:''}<div class="nmeta">${a.published?ago(a.published):''}</div></a>`).join(''):`<div class="empty">No headlines yet.</div>`)+(diag.length?`<div class="diagList">${diag.filter(d=>String(d).toLowerCase().includes('news')).map(d=>`<div>${esc(d)}</div>`).join('')}</div>`:'')}



/* ===== UI PATCH: model dashboard polish only; data untouched ===== */
function _modelEdgeKind(pr){if(!pr||pr.edge==null)return'level';return pr.edge>=6?'value':pr.edge<=-6?'fade':'level'}
function _modelSortScore(m){const pr=m.prediction||{};const live=m.status==='LIVE'?1000:0;const upcoming=isVisibleUpcoming(m)?500:0;const edge=pr.edge==null?0:Math.max(0,pr.edge)*10;const conf=Number(pr.confidence)||0;return live+upcoming+edge+conf}
function _modelMarketText(m,side){const mk=(m.markets||{})['1x2']||{};const v={h:mk.home_pct,d:mk.draw_pct,a:mk.away_pct}[side];return v==null?'market n/a':`${v}% market`}
function _modelWhen(m){if(m.status==='LIVE')return `LIVE ${m.minute||''}'`;if(m.status==='FINISHED')return 'Finished';if(isStaleUpcoming(m))return 'Past kickoff';return kickIn(m.kickoff)}
function _modelTag(m){const pr=m.prediction||{},kind=_modelEdgeKind(pr);if(m.status==='LIVE')return {txt:'LIVE',kind:'live'};if(kind==='value')return {txt:'VALUE',kind:'value'};if(kind==='fade')return {txt:'CAUTION',kind:'fade'};if((Number(pr.confidence)||0)>=65)return {txt:'HIGH CONF',kind:'level'};return {txt:'MODEL',kind:'level'}}
function _modelBars(pr){const md=pr.model||pr.blend||{};const rows=[['H','home',md.h],['D','draw',md.d],['A','away',md.a]];return `<div class="modelBars">${rows.map(([lab,cls,val])=>{val=Math.max(0,Math.min(100,Number(val)||0));return `<div class="modelBarLine"><span>${lab}</span><div class="modelBarTrack"><span class="modelBarFill ${cls}" style="width:${Math.max(2,val)}%"></span></div><span>${Math.round(val)}%</span></div>`}).join('')}</div>`}
function _modelRow(m){const pr=m.prediction||{},kind=_modelEdgeKind(pr),tag=_modelTag(m);const edge=pr.edge==null?'':`${pr.edge>0?'+':''}${pr.edge}`;return `<div class="modelRow" onclick="openMatchModal('${esc(String(m.id||''))}')"><div class="modelMatch"><div class="teams">${esc(m.home?.code||m.home?.name||'H')} v ${esc(m.away?.code||m.away?.name||'A')}</div><div class="meta">${esc(m.stage||'Fixture')} · ${_modelWhen(m)}</div></div><div class="modelChoice"><div class="small">Pick</div><div class="main">${esc(pr.pick_name||'No pick')}</div><div class="sub">${pr.confidence??'—'}% · ${esc(_modelMarketText(m,pr.pick))}</div></div>${_modelBars(pr)}<div class="modelStatus"><span class="tag ${tag.kind}">${tag.txt}</span>${edge?`<span class="tag ${kind}">${edge} edge</span>`:''}</div></div>`}
function _modelSpotlight(list){const m=list.find(x=>(x.prediction?.edge||0)>=6)||list[0];if(!m)return'';const pr=m.prediction||{},kind=_modelEdgeKind(pr);const edge=pr.edge==null?'No edge data':`${pr.edge>0?'+':''}${pr.edge} vs market`;return `<div class="modelSpot"><div class="modelSpotHead"><span>Best current read</span><span>${esc(m.stage||'Fixture')} · ${_modelWhen(m)}</span></div><div class="modelSpotBody"><div class="modelSpotTeam"><span class="code">${esc(m.home?.code||'HOME')}</span><div class="name">${esc(m.home?.name||'Home')}</div></div><div class="modelPickDial"><div class="lbl">Model pick</div><div class="pickName">${esc(pr.pick_name||'No pick')}</div><div class="conf">${pr.confidence??'—'}%</div><span class="edgePill ${kind}">${esc(edge)}</span></div><div class="modelSpotTeam away"><span class="code">${esc(m.away?.code||'AWAY')}</span><div class="name">${esc(m.away?.name||'Away')}</div></div></div></div>`}
function _modelApplyFilter(all){const f=window.MODEL_FILTER||'action';return all.filter(m=>{const pr=m.prediction||{},edge=Number(pr.edge)||0,conf=Number(pr.confidence)||0,hasOdds=!!((m.markets||{})['1x2']);if(f==='all')return true;if(f==='action')return m.status==='LIVE'||isVisibleUpcoming(m);if(f==='live')return m.status==='LIVE';if(f==='upcoming')return isVisibleUpcoming(m);if(f==='value')return edge>=6;if(f==='caution')return edge<=-6;if(f==='high')return conf>=65;if(f==='odds')return hasOdds;if(f==='finished')return m.status==='FINISHED';return true})}
function _modelFilterBtn(key,label,count){const on=(window.MODEL_FILTER||'action')===key;return `<button class="chip ${on?'on':''}" onclick="window.MODEL_FILTER='${key}';renderEdge()">${label}<span class="count">${count}</span></button>`}
function renderEdge(){const host=$('#view-edge');const all=(DATA.matches||[]).filter(m=>m.prediction).sort((a,b)=>_modelSortScore(b)-_modelSortScore(a));if(!all.length){host.innerHTML=`<div class="vhead">Model</div><div class="empty">No model picks yet.</div>`;return}if(!window.MODEL_FILTER)window.MODEL_FILTER='action';const counts={all:all.length,action:all.filter(m=>m.status==='LIVE'||isVisibleUpcoming(m)).length,live:all.filter(m=>m.status==='LIVE').length,upcoming:all.filter(isVisibleUpcoming).length,value:all.filter(m=>(m.prediction?.edge||0)>=6).length,caution:all.filter(m=>(m.prediction?.edge||0)<=-6).length,high:all.filter(m=>(Number(m.prediction?.confidence)||0)>=65).length,odds:all.filter(m=>!!((m.markets||{})['1x2'])).length,finished:all.filter(m=>m.status==='FINISHED').length};const list=_modelApplyFilter(all).sort((a,b)=>_modelSortScore(b)-_modelSortScore(a));const actionable=all.filter(m=>m.status==='LIVE'||isVisibleUpcoming(m));const value=all.filter(m=>(m.prediction?.edge||0)>=6);const caution=all.filter(m=>(m.prediction?.edge||0)<=-6);const high=all.filter(m=>(Number(m.prediction?.confidence)||0)>=65);let html=`<div class="modelShell"><div class="modelHero"><div><div class="modelHeroTitle">Model command center</div><div class="modelHeroSub">Model picks, market edge, and confidence.</div></div><div class="modelKpis"><div class="modelKpi"><span>Actionable</span><b>${actionable.length}</b></div><div class="modelKpi good"><span>Value edges</span><b>${value.length}</b></div><div class="modelKpi warn"><span>High confidence</span><b>${high.length}</b></div><div class="modelKpi bad"><span>Caution</span><b>${caution.length}</b></div></div></div><div class="modelToolbar">${_modelFilterBtn('action','Actionable',counts.action)}${_modelFilterBtn('live','Live',counts.live)}${_modelFilterBtn('upcoming','Upcoming',counts.upcoming)}${_modelFilterBtn('value','Value',counts.value)}${_modelFilterBtn('caution','Caution',counts.caution)}${_modelFilterBtn('high','High confidence',counts.high)}${_modelFilterBtn('odds','With odds',counts.odds)}${_modelFilterBtn('finished','Finished',counts.finished)}${_modelFilterBtn('all','All',counts.all)}</div>${_modelSpotlight(list.length?list:all)}<div class="modelGrid"><section class="modelPanel"><div class="modelPanelHead"><h3>Pick board</h3><span>${list.length} shown</span></div><div class="modelList">${list.length?list.map(_modelRow).join(''):`<div class="modelEmptySmall">No matches in this filter.</div>`}</div></section><section class="modelPanel"><div class="modelPanelHead"><h3>Quick reads</h3><span>digest</span></div><div class="modelReadList">`;const reads=(list.length?list:all).slice(0,5);html+=reads.map((m,i)=>{const pr=m.prediction||{},tag=_modelTag(m);return `<div class="modelRead" onclick="openMatchModal('${esc(String(m.id||''))}')"><div class="rtitle"><span>${tag.txt}</span>${esc(m.home?.code||'H')} v ${esc(m.away?.code||'A')}</div><p>${esc(edgeBreakdown(m)||`${pr.pick_name||'Model'} at ${pr.confidence||'—'}%.`)}</p></div>`}).join('');html+=`</div></section></div></div>`;host.innerHTML=html}

applySettings();setView(SETTINGS.defaultView||'matches');load();
